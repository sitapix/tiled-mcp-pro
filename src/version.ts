export const SERVER_NAME = "tiled-mcp-pro";
export const SERVER_VERSION = "0.1.0";
/**
 * Human-facing presentation for `Implementation`. `title` is the display name
 * clients show in place of the programmatic `name`; `description` was added in
 * MCP 2025-11-25 to give hosts context at initialize time. Older clients ignore
 * both.
 */
export const SERVER_TITLE = "TiledMCP Pro";
export const SERVER_DESCRIPTION =
  "Inspects and edits Tiled map assets (TMJ/TSJ/TMX/TSX/TX) under one project root. Every mutation is planned as an expiring change set and applied only on explicit approval.";
